import 'package:flutter/material.dart';

class CreatePlanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Plan'),
      ),
      body: Container(
        padding: EdgeInsets.all(30),
        child: Text('Create Plan widget goes here.'),
      ),
    );
  }
}
